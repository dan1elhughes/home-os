[Unit]
Description=Run Docker prune weekly

[Timer]
# Randomised start minute per host to avoid all nodes running simultaneously
OnCalendar=Sun *-*-* 03:{{ "%02d" | format(60 | random(seed=inventory_hostname)) }}:00
Persistent=true

[Install]
WantedBy=timers.target
