---
services:
  pairdrop:
    container_name: pairdrop
    image: lscr.io/linuxserver/pairdrop:latest
    environment:
      - PUID=${PUID?error}
      - PGID=${PGID?error}
      - TZ=Europe/London
      - WS_FALLBACK=true #optional
    networks:
      - main
